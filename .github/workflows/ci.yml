name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dotnet:
    name: .NET tests (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore PassKee.sln

      - name: Test
        run: dotnet test PassKee.sln --no-restore --nologo --verbosity normal

  rust:
    name: Rust tests (Linux)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/PassKee.Provider
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/PassKee.Provider

      - name: Test
        run: cargo test --all-targets
